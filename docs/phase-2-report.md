# Phase 2 正式 UI 报告

更新日期：2026-09-11

Phase 2 Gate 已通过。正式中文 UI 已连接真实离线引擎，覆盖输入、口音、音色、重复次数、高级设置、生成、播放和 WAV 下载闭环。

## 界面与响应式

- 双栏桌面布局，窄屏自动切换单栏。
- 使用系统字体和 inline SVG，不加载字体或图标资源。
- 6 个本地音色：美式女声 2、美式男声 2、英式女声 1、英式男声 1。
- 高级设置默认折叠，数值随滑杆同步显示。
- 1280×720 自动化检查无横向滚动，生成按钮和高级设置可访问。
- 视觉截图：`artifacts/phase2-1280x720.png`。

## Gate 实测

- `file://`：通过。
- 浏览器离线：通过。
- HTTP/HTTPS 请求：0。
- 美式/英式过滤：4 / 2 个 Voice Card。
- 英式 `bf_emma` 真实生成：通过。
- 输出：304,800 samples、24 kHz、12.7 秒、609,644 bytes。
- HTMLAudioElement 读取时长：12.7 秒。
- Blob WAV 下载：通过。

Phase 2 产物随后在同一正式 UI 上继续演进为 Phase 3，Phase 2 回归脚本保留在 `scripts/test-phase2.mjs`。
