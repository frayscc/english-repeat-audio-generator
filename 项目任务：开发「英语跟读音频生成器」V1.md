我补充核实了一点：目前官方 ONNX Community 已有更新的 **Kokoro-82M-v1.0-ONNX**，支持 JavaScript/Kokoro.js；量化模型中 `model_q8f16.onnx` 约 **86 MB**，`model_quantized.onnx` 约 **92.4 MB**，并提供 28 种美式/英式男女声，因此下面的 Prompt 会要求 Codex **优先评估 v1.0 + q8/q8f16**，而不是机械使用旧版。 

可直接整段交给 Codex：

# **项目任务：开发「英语跟读音频生成器」V1**

你现在作为本项目的**高级全栈工程师、Web 音频工程师和本地 AI 推理工程师**，负责从零开始完成一个可以实际交付给中学英语教师使用的本地英语 TTS 工具。

不要只完成 Demo。

目标是完成一个：

**完全离线、无需安装、无需服务器、无需账号、双击 HTML 即可使用的英语跟读 WAV 音频生成器。**

项目必须按照以下 Phase 顺序开发：

```text
Phase 0  技术可行性验证
↓
Phase 1  核心 TTS 与音频引擎
↓
Phase 2  正式 UI
↓
Phase 3  完整产品功能
↓
Phase 4  完全离线打包
↓
Phase 5  验收与发布
```

每一个 Phase 都必须：

1. 完成代码；
2. 自行运行测试；
3. 修复发现的问题；
4. 更新项目文档；
5. 输出当前阶段完成情况；
6. 满足 Gate 后才能进入下一阶段。

------

# **一、产品背景**

这是一个面向中国中学英语教师的小工具。

教师通常会准备一个单元的：

- 单词；
- 短语；
- 英语句子。

例如：

```text
1. environment
2. protect the environment
3. take part in
4. instead of
5. look forward to
```

程序自动清理序号后，将其识别为五个独立词条。

教师可以选择：

- 美式英语 / 英式英语；
- 男声 / 女声；
- 具体音色；
- 每个词条重复 1 / 2 / 3 次；
- 朗读速度；
- 同一词条重复之间的停顿；
- 不同词条之间的停顿。

例如选择重复两次后：

```text
environment
[1.2 s]
environment

[2.0 s]

protect the environment
[1.2 s]
protect the environment

[2.0 s]

take part in
...
```

最后将所有语音合成为：

```text
一个完整 WAV 文件
```

供教师在：

- Windows 教室电脑；
- PPT；
- 播放器；
- 剪辑软件

中直接使用。

------

# **二、最高优先级产品原则**

优先级必须严格按照：

```text
稳定
>
完全离线
>
双击即用
>
自然准确的英语发音
>
操作简单
>
生成速度
>
界面视觉
>
高级功能
```

如果任何技术方案与：

```text
完全离线
```

或者：

```text
双击 HTML 即用
```

发生冲突，优先保证以上两个要求。

------

# **三、绝对硬性要求**

## **3.1 完全离线**

用户使用正式发行版时：

**不得产生任何网络请求。**

禁止依赖：

```text
CDN
Google Fonts
在线字体
在线 SVG
在线 Icon
Hugging Face 在线加载
GitHub Raw
Microsoft TTS
Edge TTS
Google TTS
OpenAI API
任何云 API
任何在线统计服务
```

包括：

```text
HTML
CSS
JavaScript
WASM
ONNX
Tokenizer
Voice
字体
图标
配置
```

全部必须存在于本地发行物中。

浏览器开发者工具 Network 面板在正常使用过程中：

不应出现任何 HTTP/HTTPS 请求。

------

# **3.2 双击 HTML 即用**

最终正式使用方式：

```text
双击 EnglishReader.html
↓
Chrome / Edge 打开
↓
直接使用
```

用户不能被要求：

```text
安装 Node.js
安装 Python
安装 Docker
安装 npm
启动 localhost
运行 npm run dev
运行 python -m http.server
启动 exe 后台服务器
运行 bat 启动 Web Server
配置环境变量
```

开发阶段当然可以使用：

```text
Node.js
Vite
TypeScript
构建工具
```

但这些只能用于：

开发和 build。

正式产物必须不依赖这些东西。

------

# **3.3 file:// 是正式运行环境**

正式运行协议为：

```text
file://
```

例如：

```text
file:///D:/英语跟读生成器/EnglishReader.html
```

因此不能假定存在：

```text
http://localhost
```

必须主动处理：

- CORS；
- Worker；
- WASM；
- ONNX；
- ES Module；
- fetch local file；
- WebGPU；
- AudioContext；

在 `file://` 场景下可能出现的限制。

------

# **四、技术路线**

## **4.1 TTS 首选**

优先验证：

```text
Kokoro-82M-v1.0-ONNX
```

推荐优先考察：

```text
kokoro-js
```

以及必要时：

```text
@huggingface/transformers
ONNX Runtime Web
```

不要预设某一个库一定能满足最终 `file://` 要求。

Phase 0 必须实测。

------

# **4.2 模型量化**

不要默认使用 FP32。

优先评估：

```text
q8f16
q8
```

目标是：

在发音质量几乎无明显下降的情况下尽量缩小发行体积。

优先测试：

```text
model_q8f16.onnx
```

和：

```text
model_quantized.onnx
```

如果设备兼容性存在差异，应以：

普通 Windows 学校电脑兼容性

优先。

不得仅因为在开发机器上速度快就确定最终模型。

------

# **4.3 加速策略**

目标优先：

```text
WebGPU
```

如果不可用：

```text
WASM / CPU fallback
```

用户层不要暴露复杂选项。

UI 只需显示：

```text
GPU 加速
```

或：

```text
兼容模式
```

------

# **五、工程原则**

建议代码结构在开发阶段保持模块化，例如：

```text
src/
├── app/
├── tts/
│   ├── engine.ts
│   ├── voices.ts
│   └── model.ts
├── audio/
│   ├── mixer.ts
│   ├── silence.ts
│   ├── resample.ts
│   └── wav.ts
├── parser/
│   └── textParser.ts
├── state/
├── ui/
└── utils/
```

不要因为最终要输出单 HTML 就把源代码写成一个巨大 JS 文件。

开发源码：

保持清晰、模块化、可维护。

构建阶段再生成最终发行物。

------

# **六、Phase 0 — 技术可行性验证**

## **目标**

这一阶段是整个项目最重要的 Gate。

**不要开发正式 UI。**

只验证：

Kokoro 是否可以在完全离线的 `file://` 页面中完成英语 TTS。

------

## **Phase 0.1 建立最小项目**

创建最简单测试页：

```text
test.html
```

界面只需要：

```text
[text input]

[初始化模型]

[生成]

<audio controls>
```

测试文本：

```text
environment
```

------

## **Phase 0.2 本地模型**

禁止：

```javascript
from_pretrained("onnx-community/...")
```

直接联网下载模型作为最终方案。

必须把模型下载到项目资源中。

研究并实现：

让运行时从本地数据读取模型。

------

# **Phase 0.3 重点研究 file:// 限制**

必须实际研究并验证：

### **A**

JS bundle 能否直接运行。

### **B**

WASM 是否能正常加载。

### **C**

ONNX 二进制模型是否能读取。

### **D**

Voice 数据是否能读取。

### **E**

Worker 是否能运行。

### **F**

WebGPU 在 `file://` 页面是否可用。

### **G**

WASM fallback 是否可用。

------

# **Phase 0.4 必要时采用资源内嵌**

如果浏览器由于：

```text
file://
```

无法可靠读取：

```text
model.onnx
*.wasm
voice.bin
tokenizer.json
```

不要立刻放弃双击 HTML 要求。

研究采用：

```text
Base64
Uint8Array
Blob
ArrayBuffer
Blob URL
Data URL
构建时内嵌
```

等方法。

允许最终 HTML 文件体积较大。

------

# **Phase 0.5 单文件优先**

正式发行目标优先级：

### **最佳**

```text
EnglishReader.html
```

一个文件包含：

```text
UI
CSS
JS
WASM
模型
Tokenizer
所需 Voice
```

直接复制该文件即可使用。

如果技术上由于浏览器限制确实无法实现，应提供：

1. 明确技术证明；
2. 已尝试的方法；
3. 失败原因；
4. 最接近要求的替代架构。

但是：

不允许仅因为实现麻烦，就退回 localhost。

------

# **Phase 0.6 不需要打包全部 28 个 Voice**

正式产品只准备：

```text
6～8 个
```

经过筛选的高质量英语 Voice。

减少发行体积。

至少：

```text
American Female × 2
American Male × 2
British Female × 1
British Male × 1
```

优先从 Kokoro 官方 Voice 中挑选。

------

# **Phase 0.7 测试音色**

创建自动测试或人工测试表。

至少测试：

```text
environment
comfortable
vegetable
interesting
schedule
water
can't
advertisement
look forward to
protect the environment
I'd like to have a try.
```

重点检查：

- 单词重音；
- 美英口音差异；
- 短语自然程度；
- 缩写；
- 撇号；
- 句子语调。

------

# **Phase 0 Gate**

Phase 0 完成的硬性条件：

断开电脑网络后：

```text
双击 test.html
↓
模型初始化
↓
输入 environment
↓
本地生成音频
↓
正常播放
```

至少在：

```text
Chrome
Edge
```

中验证。

成功以后才能开始 Phase 1。

------

# **Phase 0 结束时输出**

生成：

```text
docs/phase-0-report.md
```

记录：

```text
选用 TTS 版本
选用模型文件
模型大小
运行库
WASM 方案
WebGPU 情况
file:// 是否成功
Chrome 测试
Edge 测试
首次模型初始化时间
首次单词生成时间
后续生成时间
内存占用
遇到的问题
最终技术决定
```

------

# **七、Phase 1 — 核心音频引擎**

Phase 0 成功后再开始。

此阶段仍然：

功能优先，不做复杂 UI。

------

# **Phase 1.1 TTS Engine**

设计统一接口，例如：

```typescript
interface TTSEngine {
  initialize(): Promise<void>;

  synthesize(
    text: string,
    options: {
      voice: string;
      speed: number;
    }
  ): Promise<AudioData>;

  dispose(): Promise<void>;
}
```

UI 不应直接调用底层 Kokoro API。

------

# **Phase 1.2 Voice 定义**

建立统一数据结构：

```typescript
interface VoiceProfile {
  id: string;
  internalId: string;
  name: string;
  locale: "en-US" | "en-GB";
  gender: "female" | "male";
  description: string;
}
```

例如内部：

```text
af_heart
```

UI 可以展示：

```text
Emma
自然清晰 · 女声
```

不要展示技术 ID。

------

# **Phase 1.3 文本解析**

实现：

```typescript
parseInput(rawText)
```

输入：

```text
1. environment

2、protect the environment

③ take part in

四、instead of

（5）look forward to
```

输出：

```json
[
  "environment",
  "protect the environment",
  "take part in",
  "instead of",
  "look forward to"
]
```

------

# **Phase 1.4 清洗规则**

支持删除：

```text
1.
1、
1)
(1)
（1）
① ② ③ ...
一、
二、
三、
```

自动：

- Trim；
- 删除空行；
- 保持原有英文大小写；
- 保持标点；
- 保持撇号。

禁止自动：

- 拼写修复；
- 翻译；
- 改单复数；
- 改大小写；
- 删除英文标点。

------

# **Phase 1.5 单词缓存**

同一个文本：

```text
environment
```

重复两遍，不允许运行两次 TTS。

逻辑：

```text
TTS(environment)
↓
AudioBuffer A

A
+
Silence
+
A
```

建立当前任务级缓存：

```text
text
voice
speed
```

作为 cache key。

------

# **Phase 1.6 音频拼接**

必须自己控制停顿。

不要：

```text
environment environment protect...
```

一次性传给模型。

实现：

```text
TTS(text 1)
+
silence repeat
+
TTS(text 1)
+
silence item
+
TTS(text 2)
...
```

------

# **Phase 1.7 参数**

默认：

```text
repeatCount = 2
repeatGap = 1.2 s
itemGap = 2.0 s
speed = 0.90
```

范围：

```text
repeatCount: 1～3

repeatGap:
0.3～5.0 s

itemGap:
0.5～10.0 s

speed:
0.70～1.20
step 0.05
```

------

# **Phase 1.8 最后一个条目**

最后一个条目朗读结束以后：

不需要额外添加 2 秒词条间隔。

避免导出的 WAV 尾部出现明显无意义空白。

------

# **Phase 1.9 WAV Encoder**

实现本地 PCM WAV 编码。

优先：

```text
PCM
16-bit
Mono
```

采样率：

优先保持 Kokoro 输出原始采样率。

如果需要统一：

采用高质量重采样，而不是简单抽样。

不要为了满足 44.1 kHz 而无意义降低音质。

------

# **Phase 1.10 测试**

至少增加针对：

```text
parser
silence
repeat
audio duration
wav header
cache
```

的自动测试。

例如：

```text
2 个词
重复 2 次
每段语音 1 秒
repeat gap 1 秒
item gap 2 秒
```

理论总时长必须可以计算并验证。

------

# **Phase 1 Gate**

必须能：

```text
输入多个单词
↓
生成每个独立 TTS
↓
重复
↓
插入正确 silence
↓
合并
↓
生成 WAV
↓
WAV 可以正常播放
```

之后再开始 UI。

------

# **八、Phase 2 — 正式 UI**

目标：

一个普通英语教师不看说明书也能使用。

------

# **Phase 2.1 视觉风格**

整体：

```text
现代
简洁
轻量
专业
大量留白
柔和圆角
清晰层级
```

禁止：

```text
AI 科技风
荧光渐变
玻璃拟态泛滥
复杂动画
巨量阴影
仪表盘式 UI
```

------

# **Phase 2.2 主界面**

结构：

```text
┌───────────────────────────────────────┐
│ 英语跟读音频生成器                    │
│ 本地运行 · 完全离线                   │
├───────────────────────────────────────┤

 单词 / 短语 / 句子

 ┌─────────────────────────────────────┐
 │ environment                         │
 │ protect the environment             │
 │ take part in                        │
 │                                     │
 └─────────────────────────────────────┘

 共 3 条

 发音
 [美式英语] [英式英语]

 音色
 [Voice Cards...]

 重复次数
 [1次] [2次] [3次]

 ▾ 高级设置

             [生成音频]

─────────────────────────────────────────

 ▶  00:00 ━━━━━━━━━━━━━ 02:34

             [导出 WAV]
```

------

# **Phase 2.3 默认配置**

默认：

```text
美式英语
推荐女声
重复 2 次
0.90×
同词 1.2 秒
词间 2.0 秒
```

做到：

用户只粘贴文本，再点击“生成音频”即可。

------

# **Phase 2.4 音色选择**

按照：

```text
美式英语
英式英语
```

过滤 Voice。

每个 Voice Card 显示：

```text
Emma
女声 · 自然清晰

▶ 试听
```

不要显示：

```text
af_heart
```

等内部信息。

------

# **Phase 2.5 高级设置**

默认折叠。

包含：

```text
朗读速度
同一词条重复间隔
不同词条之间间隔
恢复默认设置
```

参数修改立即显示数字。

------

# **Phase 2.6 模型状态**

页面顶部或合适区域显示：

初始化：

```text
正在准备语音模型…
```

完成：

```text
语音模型已就绪
```

模式：

```text
GPU 加速
```

或：

```text
兼容模式
```

避免显示：

```text
execution provider
onnx
wasm threads
```

------

# **Phase 2.7 响应式**

重点：

```text
1280×720
1366×768
1920×1080
```

不能：

- 按钮跑出屏幕；
- 产生横向滚动；
- 高级设置无法访问。

手机不是重点。

------

# **Phase 2 Gate**

必须完成完整视觉界面。

此时即使部分高级功能尚未接入，也必须完成：

```text
输入
↓
配置
↓
生成
↓
播放
↓
下载
```

基本闭环。

------

# **九、Phase 3 — 完整产品功能**

## **Phase 3.1 实时词条统计**

例如：

```text
共 48 条
```

根据清洗后的有效条目统计。

------

# **Phase 3.2 单条预览**

解析后允许显示词条预览。

例如：

```text
environment                ▶
protect the environment    ▶
take part in               ▶
```

点击：

```text
▶
```

只生成并播放这一条。

如果已经缓存：

直接播放缓存，不重新推理。

------

# **Phase 3.3 音色试听**

使用固定试听语句，例如：

```text
Hello! This is a sample of this English voice.
```

切换 Voice 后可直接试听。

允许缓存试听结果。

------

# **Phase 3.4 生成进度**

生成 48 条时显示：

```text
正在生成音频

12 / 48

protect the environment

████████░░░░░░░ 25%
```

进度应对应：

实际已经完成的 TTS 条目。

------

# **Phase 3.5 取消**

生成期间显示：

```text
取消生成
```

取消后：

- 停止后续生成；
- 释放临时资源；
- 保留可安全复用缓存；
- UI 恢复正常。

------

# **Phase 3.6 错误恢复**

单条失败：

```text
第 16 条生成失败

protect the environment
```

提供：

```text
重试
```

不要让已经完成的前 15 条作废。

------

# **Phase 3.7 播放器**

至少支持：

```text
播放
暂停
当前时间
总时长
进度条
拖动
音量
重新播放
```

------

# **Phase 3.8 WAV 导出**

按钮：

```text
导出 WAV
```

默认文件名：

```text
英语跟读_YYYY-MM-DD_HHmm.wav
```

例如：

```text
英语跟读_2026-09-11_1930.wav
```

------

# **Phase 3.9 设置保存**

使用：

```text
localStorage
```

保存：

```text
accent
voice
repeat count
speed
repeat gap
item gap
```

不要保存：

```text
输入的英语内容
生成的 WAV
```

------

# **Phase 3.10 恢复默认值**

提供：

```text
恢复默认设置
```

恢复：

```text
美式
默认推荐女声
重复 2
0.9×
1.2 s
2.0 s
```

------

# **Phase 3.11 合理限制**

为防止浏览器内存爆炸，评估：

```text
最大条数
最大总字符
预计 WAV 时长
```

不要无条件允许用户一次生成几千条。

建议先研究实际内存占用再决定。

如果超过安全范围：

使用友好提示，例如：

```text
当前内容较多，可能占用较多内存。
建议分批生成。
```

而不是直接崩溃。

------

# **Phase 3 Gate**

完成所有 V1 产品功能。

------

# **十、Phase 4 — 完全离线打包**

这一阶段非常重要。

------

# **Phase 4.1 清除所有网络依赖**

搜索整个工程：

```text
http://
https://
cdn
unpkg
jsdelivr
huggingface.co
githubusercontent
fonts.googleapis
```

正式 bundle 中不得存在运行时网络依赖。

------

# **Phase 4.2 本地 SVG**

所有 Icon：

优先：

```text
inline SVG
```

不使用在线 icon font。

------

# **Phase 4.3 字体**

使用：

```css
-apple-system,
BlinkMacSystemFont,
"Segoe UI",
"Microsoft YaHei",
Arial,
sans-serif
```

不加载网络字体。

------

# **Phase 4.4 单 HTML 构建**

优先实现：

```text
dist/EnglishReader.html
```

一个文件包含：

```text
HTML
CSS
JS
必要 WASM
Tokenizer
TTS 模型
精选 Voice
```

如果因为模型大导致构建工具默认限制，需要修改构建脚本。

不得因为：

```text
文件 100MB+
```

就放弃单文件方案。

大 HTML：

可以接受。

网络依赖：

不可接受。

------

# **Phase 4.5 内存优化**

注意：

Base64 会增加磁盘体积，同时直接 decode 可能造成：

```text
Base64 String
+
Uint8Array
+
ONNX Runtime Copy
```

同时存在导致内存浪费。

因此要研究：

- 是否可以分块 decode；
- 尽早释放大字符串引用；
- Blob；
- ArrayBuffer；
- transferable；
- 避免多份模型副本。

普通 8 GB Windows 电脑必须合理运行。

------

# **Phase 4.6 Source Map**

正式发行版：

不要内嵌巨大 source map。

可以：

```text
开发版有 source map
发行版关闭
```

------

# **Phase 4.7 License**

保留所有依赖的开源许可证。

生成：

```text
THIRD_PARTY_LICENSES.md
```

特别确认：

```text
Kokoro
kokoro-js
Transformers.js
ONNX Runtime
```

等实际使用项目的 License。

------

# **Phase 4 Gate**

创建最终发行：

```text
release/
├── EnglishReader.html
├── README.txt
└── THIRD_PARTY_LICENSES.txt
```

如果能够真正单文件，则 HTML 是唯一运行文件。

README 仅作说明，不是程序依赖。

------

# **十一、Phase 5 — 验收**

不要只声明：

```text
Done
```

必须真正测试。

------

# **Phase 5.1 完全离线测试**

关闭：

```text
Wi-Fi
Ethernet
```

然后：

```text
双击 HTML
↓
初始化模型
↓
切换 Voice
↓
试听
↓
输入文本
↓
生成
↓
播放
↓
导出 WAV
```

全部正常。

------

# **Phase 5.2 Network 验证**

在联网情况下打开 DevTools：

```text
Network
```

重新加载应用并完整生成一次音频。

要求：

```text
HTTP / HTTPS 外部请求 = 0
```

------

# **Phase 5.3 中文路径**

复制到：

```text
D:\教学资料\英语跟读工具\
```

测试。

------

# **Phase 5.4 带空格路径**

例如：

```text
D:\My Teaching Tools\English Reader\
```

测试。

------

# **Phase 5.5 U 盘场景**

如果有可用环境：

从移动存储直接打开。

至少保证代码设计不能依赖固定盘符。

------

# **Phase 5.6 浏览器**

测试：

```text
最新稳定版 Edge
最新稳定版 Chrome
```

Windows 为第一优先。

macOS Chrome 作为附加测试。

------

# **Phase 5.7 GPU 与 CPU**

至少验证：

### **环境 A**

WebGPU 可用。

### **环境 B**

WebGPU 禁用或者不可用。

环境 B 必须可以：

```text
自动 fallback
```

而不是程序不可用。

------

# **Phase 5.8 文本解析**

测试：

```text
1. environment

2、protect the environment

③ take part in

四、instead of

（5）look forward to
```

结果必须准确为 5 条。

------

# **Phase 5.9 特殊字符**

测试：

```text
don't
I'm
I'd like to
mother-in-law
U.S.
Mr. Smith
How are you?
```

保证不会因为 parser 误处理。

------

# **Phase 5.10 100 条压力测试**

至少准备：

```text
100 条
```

混合：

- 单词；
- 短语；
- 短句。

测试：

```text
重复 2 次
```

确保：

- 不崩溃；
- 不乱序；
- 不漏项；
- 不重复错误；
- WAV 可以打开；
- 停顿正确。

------

# **Phase 5.11 重复测试**

分别：

```text
1 次
2 次
3 次
```

检查实际音频。

------

# **Phase 5.12 停顿测试**

修改：

```text
repeatGap
itemGap
```

实际 WAV 时长变化必须符合数学预期。

------

# **Phase 5.13 WAV 验证**

至少在：

```text
Windows Media Player / Media Player
Chrome
Edge
macOS QuickTime（如果有环境）
```

验证。

同时程序化检查：

```text
RIFF
WAVE
fmt
data
```

Header 是否正确。

------

# **Phase 5.14 内存测试**

记录：

```text
应用初始化后内存
首次生成后
100 条生成过程中峰值
完成后
```

避免每生成一个词内存持续无限增长。

检查：

```text
Blob URL revoke
AudioBuffer 生命周期
临时 ArrayBuffer
缓存
```

------

# **Phase 5.15 长时间重复使用**

连续完成：

```text
生成
→
修改文本
→
再次生成
→
再次导出
```

至少 5 次。

不得必须刷新页面才能继续。

------

# **十二、UI 文案**

统一使用中文。

例如：

```text
英语跟读音频生成器

本地运行 · 完全离线

单词 / 短语 / 句子

共 28 条

美式英语
英式英语

重复次数

高级设置

朗读速度

同一词条重复间隔

不同词条间隔

恢复默认设置

生成音频

取消生成

正在准备语音模型…

语音模型已就绪

GPU 加速

兼容模式

导出 WAV
```

错误提示要对普通教师友好。

不要显示大量 Stack Trace。

开发模式 Console 可以保留详细错误。

------

# **十三、明确不做的事情**

V1 不开发：

```text
中文 TTS
多语言系统
MP3
AAC
云 API
用户登录
账号
数据库
服务器
云同步
学生管理
单词释义
翻译
IPA
例句生成
AI 功能
ChatGPT
PPT
视频
字幕
SRT
```

不要主动扩 Scope。

------

# **十四、代码质量要求**

必须：

- TypeScript 尽可能开启 strict；
- 避免 any 滥用；
- UI 与 Engine 解耦；
- 解析器可独立测试；
- WAV 编码器可独立测试；
- Audio Mixer 可独立测试；
- 模型路径/模型信息集中配置；
- Voice 映射集中配置；
- 不在组件中散落 magic number。

默认配置集中，例如：

```typescript
export const DEFAULT_SETTINGS = {
  locale: "en-US",
  repeatCount: 2,
  speed: 0.9,
  repeatGapSeconds: 1.2,
  itemGapSeconds: 2.0,
};
```

------

# **十五、不要过度设计**

不要引入：

```text
Redux
复杂状态机框架
后端
数据库
Electron
Tauri
Docker
Service Mesh
微服务
```

除非有不可替代的技术原因。

这个产品本质是：

一个本地浏览器工具。

------

# **十六、开发方式**

请主动工作，不要每一个小决定都询问用户。

对普通工程问题：

自行选择合理方案并继续。

只有出现会改变产品核心要求的技术问题时，才需要停下来说明。

例如：

```text
经过实际测试，Chrome 明确禁止某能力，
导致单 HTML + file:// 无法实现，
且所有合理 workaround 均失败。
```

这种情况应：

1. 给出实验结果；
2. 给出浏览器错误；
3. 给出尝试方案；
4. 给出替代方案；
5. 等待产品决策。

不得未经验证就说：

浏览器不支持。

------

# **十七、每个 Phase 的 Git 管理**

如果当前目录是 Git 项目：

每完成一个 Phase 建议创建清晰 Commit。

例如：

```text
feat: complete phase 0 offline TTS spike

feat: implement core TTS and audio engine

feat: build production UI

feat: complete V1 feature set

build: produce fully offline single-file release

test: complete V1 acceptance suite
```

不要提交：

- node_modules；
- 临时缓存；
- 无意义的大模型重复文件。

模型源文件如何管理根据仓库环境决定，并记录 README。

------

# **十八、文档要求**

最终至少存在：

```text
README.md

docs/
├── architecture.md
├── phase-0-report.md
├── testing.md
└── release.md

THIRD_PARTY_LICENSES.md
```

------

# **十九、README 内容**

必须包含：

## **使用方法**

```text
1. 双击 EnglishReader.html
2. 输入单词/短语，每行一个
3. 选择英语口音和声音
4. 选择重复次数
5. 点击生成音频
6. 试听
7. 导出 WAV
```

以及：

```text
无需安装
无需联网
所有数据均在本地处理
```

不要让普通用户看到 npm/build 等开发说明作为首页主要内容。

开发文档可以单独放。

------

# **二十、最终交付标准**

最终交付必须包含：

```text
完整源代码
+
可重新构建的构建脚本
+
自动测试
+
技术文档
+
最终发行版
```

最理想：

```text
release/EnglishReader.html
```

教师只需要拿到这个文件即可。

------

# **二十一、最终汇报格式**

项目全部结束后，不要只说：

```text
完成。
```

请输出：

## **1. 最终架构**

说明：

```text
TTS 模型
量化方式
运行库
WebGPU
WASM fallback
模型嵌入方式
WAV 生成方式
```

## **2. 发行文件**

列出：

```text
release/
```

内容以及文件大小。

## **3. 浏览器兼容性**

列出：

```text
Edge
Chrome
macOS Chrome
```

测试结果。

## **4. 离线验证**

明确说明：

```text
是否断网验证
是否 Network = 0
是否 file:// 验证
```

## **5. 性能**

至少给：

```text
模型初始化时间
单词平均生成速度
100 条任务耗时
峰值内存
最终 HTML 大小
```

## **6. 已知限制**

如实列出。

## **7. 自动测试**

说明：

```text
测试数量
通过数量
失败数量
```

------

# **二十二、立即开始**

现在从：

# **Phase 0**

开始。

**不要先制作正式 UI。**

第一目标只有一个：

在完全断网环境下，通过 `file://` 双击本地 HTML，使用本地 Kokoro 英语 TTS 模型成功生成并播放 `environment`。

请先检查当前工作目录和现有项目状态。

如果目录为空，则自行初始化合理的 TypeScript/Web 项目。

然后完成 Phase 0 的实际实现、运行、测试与 `phase-0-report.md`。

只有 Phase 0 Gate 真正通过以后，才进入 Phase 1。

不要用 Mock TTS 冒充通过。

不要通过网络缓存冒充离线。

测试完全离线时必须确保模型、Voice、WASM 等所有资源都来自本地发行物。

开始工作。

这版我特意强化了一个关键点：**让 Codex 不得凭印象判断** **`file://`** **可不可行，而必须先做真实实验。** 另外，Kokoro v1.0 当前确实同时提供美式男女声和英式男女声，足以直接做你需要的口音/性别筛选。 